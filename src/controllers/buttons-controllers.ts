import { Context } from 'telegraf';

export class StartController {
  static async handle(ctx: Context) {
    try {
      // Lógica para responder ao comando de start do bot
      
    } catch (error) {
      console.error('Erro ao lidar com o comando de start:', error);
    }
  }
}
